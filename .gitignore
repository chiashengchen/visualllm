.env
__pycache__/
*.pyc
.venv/
venv/
*.log
logs/
models/
checkpoints/
*.wav
*.mp4
*.png
*.jpg
*.jpeg

# Local MuseTalk (cloned upstream + multi-GB weights + caches)
local_services/musetalk_server/vendor/
local_services/musetalk_server/avatar_cache/
# TensorRT engines (~1.75GB, GPU/driver-specific) -- rebuilt with trt_build.py, never committed
local_services/musetalk_server/trt_cache/

# Runtime artifacts (renders, probes, reports) — regenerated, not source
output/

# Avatar memory (per-user runtime state: profile, summary, session log)
state/

# Vendored CosyVoice server (weights + upstream clone + runtime are NOT committed)
tts/cosyvoice-server/CosyVoice/
tts/cosyvoice-server/outputs/
tts/cosyvoice-server/__pycache__/
tts/cosyvoice-server/*.log

# Scratch logs at repo root
scratchpad_*.log
_*.log
